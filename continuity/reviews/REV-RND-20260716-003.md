# Revisão independente — RND-20260716-003

Review-ID: `REV-RND-20260716-003`  
Charter revisado: `CHR-WP01-001`  
Rodada revisora: `RND-20260716-004`  
Revisor: ChatGPT, orquestrador  
Veredito: `ACCEPTED`

## Commits revisados

| Repositório | Baseline | Commit executado | Resultado estrutural |
|---|---|---|---|
| `faleious-ai/gitlab_ynh` | `e2d79f37aeeb765c53c9474f1ea6da3b8ac238b8` | `49b592b7e58a5a17d6a4e64d0e2567c95701c955` | um commit, somente documentação/continuidade/auditoria |
| `faleious-ai/gitlab-runner_ynh` | `00db3a0d455db06f54305fef13d6ea596ad6967e` | `80489c40e4ea073cd84c7585305ce1cef48cb1ad` | um commit, somente documentação/continuidade/auditoria |

Os commits usam o mesmo `Round-ID: RND-20260716-003`, não criam branch e não modificam manifest, scripts, configurações ou versão.

## Cobertura do charter

Todos os outputs obrigatórios existem:

### GitLab

- `docs/audit/GITLAB_PACKAGE_BASELINE.md`;
- `docs/audit/UPSTREAM_DIVERGENCE.md`;
- `docs/audit/AUTOUPDATE_GAPS.md`;
- `docs/audit/LIFECYCLE_AND_RISK_MAP.md`;
- `docs/audit/CROSS_REPO_BASELINE.md`.

### Runner

- `docs/audit/RUNNER_PACKAGE_BASELINE.md`;
- `docs/audit/UPSTREAM_DIVERGENCE.md`;
- `docs/audit/AUTOUPDATE_GAPS.md`;
- `docs/audit/LIFECYCLE_AND_SECURITY_MAP.md`.

Status, handoff, active round, evidence index e round records foram atualizados nos dois repositórios.

## Reproduções do revisor

1. O manifest GitLab da fork no baseline e o manifest do snapshot upstream possuem o mesmo blob SHA e declaram `19.1.0~ynh1`.
2. A fixture Runner da fork e do snapshot upstream possuem o mesmo blob SHA e contêm um literal com formato de credencial de Runner. O valor foi tratado como segredo, não é reproduzido neste registro e não foi testado.
3. `actions.json` declara `/bin/bash scripts/actions/register`; a leitura desse path retorna ausência. O achado estrutural foi reproduzido.
4. O workflow GitLab executa diariamente, cria branch e abre PR com base `testing`; o conflito com a política master-only foi reproduzido.
5. A comparação baseline → execução mostra exatamente um commit por repositório e somente os arquivos documentais previstos.

## Avaliação de qualidade

- Os relatórios distinguem corretamente `VERIFIED`, `FAILED` e `UNVERIFIED`.
- A inexistência de host YunoHost, Docker controlado, token efêmero e dependência do package_linter não foi escondida.
- As candidatas de versão foram registradas como observação temporal e não promovidas.
- Os riscos possuem severidade, evidência, consequência e próxima prova.
- O backlog derivado preserva dependências e critérios observáveis.
- Nenhum output afirma que lifecycle real foi demonstrado.

## Riscos residuais aceitos para fechamento de WP-01

- literal credential-like ainda presente na árvore atual e no histórico público;
- possível exposição de token por argumento de processo;
- action `register` sem implementação;
- `unregister --all-runners` com efeito remoto amplo;
- Runner/helper images sem resolver atômico;
- updater GitLab incompatível com master-only;
- package_linter e lifecycle ainda não demonstrados.

Esses itens não invalidam a auditoria; constituem entradas prioritárias para implementação e assurance.

## Gate humano

`HG-RUN-SEC-01`: quem administra o projeto GitLab usado pelo package_check deve confirmar revogação, rotação ou expiração do valor histórico. O valor não deve ser usado para verificar validade.

O gate não bloqueia a remediação técnica da árvore atual nem a implementação do updater.

## Decisão

`CHR-WP01-001` está `ACCEPTED`.

A próxima rodada autorizada é `CHR-WP02-001`, com repositório primário `faleious-ai/gitlab-runner_ynh`, destinada à remediação de segurança, reparo da action e fundação determinística do updater Runner + helper images.
