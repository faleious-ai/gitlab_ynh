# Round RND-20260717-017

Charter: `CHR-PROGRAM-V2-CONTINUE-001`  \
Task: `T-GOV-ARCHITECTURE-V2-READONLY-CONSUMER`  \
Initial remote heads: coordinator `b23fda36ac0d2309b70a9f8ba2b637e9e0cc4850`; Runner `2166387871fa879449b51c00f6e3786088daef27`.

## Scope and RED/GREEN

The mandatory first correction makes the Runner consumer read-only with respect to the tracked coordinator queue. The RED test failed because the existing consumer invoked `refresh-queue` without `--queue`. The implementation added a context-managed temporary directory and passed one explicit queue path to refresh, doctor and plan. The focused suite is GREEN: 5/5 tests pass, including byte-for-byte sentinel preservation and invocation inspection.

## Governance and manifest

The correction task and finding were registered before normal consumer execution. Doctor initially exposed eight pre-existing coordinator protected-inventory drifts; their hashes were reconciled to the published bytes. The consumer and behavioral-test hashes were then recorded in the protected manifest. The inventory remains enforced.

## Parallel preparation

`validate-wave` returned `parallelism_demonstrated=true` and `valid=true`. `LANE-01` used worker `worker-consumer-contract` in `lane-gov`, with a non-empty artifact and log. `LANE-02` used worker `worker-runner-ci-baseline` in `lane-ci`, with a non-empty artifact and log; its first snapshot command assumed absent `actions.json`, was classified and recovered. Start intervals overlapped. The recovered CI lane reproduced the baseline continuity failure at 35/39 and back-propagated it to `T-RUN-CI-REPAIR-001`.

## Validation

Temporary queue refresh, doctor and plan passed; the tracked queue was unchanged. Secret scan was clean and the focused py_compile passed. Real-diff validation was executed in both repositories. It returned `ORCHESTRATOR_OWNED_PATH_CHANGED` for the explicitly authorized protected mandate/consumer/test paths, and no protection was weakened. Spec and Engineering reviews are stored beside this evidence.

## Publication boundary

Coordinator and Runner each receive one serial commit with this Task-ID and a SELF-bound receipt. The final commit IDs are obtained only after remote publication and exact fetch reconciliation. Remote CI, later planner tasks and live GitLab/MCP claims remain unverified in this task.
