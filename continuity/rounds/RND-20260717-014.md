# RND-20260717-014 — preparação orientada por acceptance tests

Round-ID: `RND-20260717-014`  
Papel: Orquestrador  
Estado: `READY_CHARTER_PREPARED`

## Baseline

- coordinator: `2224bb3bb7e1ce0f36f26e8fe8b9f278948f2761`;
- Runner: `01162a8245653bcf5115141ca64e5f6cdcfd2fdc`.

## Decisões aplicadas

- acceptance test do objetivo pertence ao Orquestrador;
- Executor usa TDD focal sob esse contrato;
- espera por revisão não bloqueia automaticamente trabalho reversível independente;
- gate de decisão exige esgotamento do trabalho independente;
- preparação paralela e integração serial;
- paralelismo precisa de lanes e sobreposição demonstrada;
- fila técnica deve atravessar work packages autorizados.

## Tarefas do Orquestrador

| Task-ID | Repositório | Commit | Resultado |
|---|---|---|---|
| `T-ORCH014-01-autonomy-acceptance` | coordinator | `690873f7ea83a7f35fe3791f88cd0f6bdec4583b` | spec e acceptance do motor publicados; RED esperado |
| `T-ORCH014-02-alpine-acceptance` | Runner | `17be5e890010c2eb96d857713f2bc0164092b943` | matriz upstream e acceptance do default publicados; RED esperado |
| `T-ORCH014-03-gitlab-autoupdate-acceptance` | coordinator | `74d4ba939b1759cbab6505fa622872ef984f05aa` | acceptance CE/EE, matriz e required stops publicado; RED esperado |
| `T-ORCH014-04-mcp-foundation-acceptance` | coordinator | `f6dd35b0b30cb72505a6e4a6d7eb0e2b689566a8` | acceptance MCP lifecycle/policy publicado; RED esperado |
| `T-ORCH014-05-charter` | cross-repo | `this task commit` | charter, status e handoff preparados |

## Incidente de escrita no coordenador

Durante T-ORCH014-01, cinco chamadas incorretas da Contents API criaram arquivos vazios no `master`: `NONEXISTENT`, `THIS_SHOULD_NOT_BE_USED`, `DO_NOT_USE_CONTENTS_API`, `STOP` e `FINAL_ERROR`.

Foram removidos por commits compensatórios sequenciais, encerrando em `4623642db189a388f74c1928e19ec70f96851393`. Busca posterior não encontrou nenhum dos paths na árvore. O histórico não foi reescrito. Após carregar explicitamente `update_ref`, todas as publicações seguintes usaram blobs/tree/commit/ref.

Esse incidente é responsabilidade do Orquestrador e não altera a avaliação do Executor.

## Fontes primárias

- Alpine support matrix: `https://www.alpinelinux.org/releases/`;
- Docker Alpine tags: `https://hub.docker.com/_/alpine/tags/`;
- GitLab upgrade paths: `https://docs.gitlab.com/update/upgrade_paths/`;
- MCP current version: `https://modelcontextprotocol.io/docs/learn/versioning`.

## Saída

`CHR-GOV-AUTONOMY-001` autoriza o próximo Executor a implementar os oracles RED, demonstrar lanes paralelas, integrar o novo sistema de trabalho e continuar pela fila técnica sem depender de nova mensagem enquanto houver tarefa elegível dentro dos limites.
